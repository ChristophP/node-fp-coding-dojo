const { Maybe, matchWith } = require("funpro");

// create a simple union type for a traffic light
// as described here https://github.com/Christophp/funpro#union-types-algebraic-data-types

const getFirstListItem = list => {
  // return Just(item) if there is one or Nothing is the list is empty
};
